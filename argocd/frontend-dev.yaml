apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: frontend-dev
  namespace: argocd

spec:
  project: default

  source:
    repoURL: https://github.com/sammyCHY/Mini_Project-Advanced_GitOps_Techniques.git
    targetRevision: main
    path: kubernetes/frontend/overlays/dev

  destination:
    name: cluster-dev
    namespace: frontend-dev

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true